Release v1.2 -> v1.3 - 2013-12-02

Guillaume Tucker (3):
      eeprom: add `skip' options + tidy-up LOG messages
      eeprom: enable EEPROM I2C address from config file
      eeprom: add addr option to help message

Release v1.1 -> v1.2 - 2013-11-18

Guillaume Tucker (3):
      rename hvpmic -> max17135
      remove log.h to use libplutil instead
      add tps65185 command for the TI TPS65185 HVPMIC

Release v1.0 -> v1.1 - 2013-10-11

Andrew Cox (1):
      Add temperature and temperature mode to low-level ePDC options.

Guillaume Tucker (7):
      eeprom: use new API with EEPROM mode string
      eeprom: add -o option parser
      update help message with new eeprom options
      use LOG_PRINT for special log message formatting
      Android.mk: fix build with new libplutil and eglib
      use new PLEP_TEMPERATURE_AUTO hardware option id
      Android.mk: fix path to libplutil

Michele Marchetti (3):
      remove default I2C bus as it's now read from the config file
      remove default I2C addresses
      update plep_init call with NULL config file path

Release v0.6 -> v1.0 - 2013-07-19

Guillaume Tucker (6):
      hvpmic: fix function to set timings
      Makefile: link agains libplsdk.so
      add epdc command to set low-level ePDC hardware options
      Android.mk: add dependency to libplepaper
      fix typo to match latest libplhw library
      add setver.sh script to set the application version string

Release v0.5 -> v0.6 - 2013-06-18

Guillaume Tucker (5):
      initial commit: import from plhwtools v0.5
      add GPL v3 license + update copyright statements
      Android.mk: list all source files to fix Android NDK build
      use getopt + add I2C block size for EEPROM transfers
      update help message

